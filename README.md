# ai-chat-kit

Headless React hooks and components for streaming LLM chat UIs. The streaming
state lives outside React, so a token re-renders one message — not the list.

Work in progress; the first release is being assembled.
